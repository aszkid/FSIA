FILE(GLOB_RECURSE SRCS "${PROJECT_SOURCE_DIR}/extlibs/*.cpp" "${PROJECT_SOURCE_DIR}/extlibs/*.c")

ADD_LIBRARY(${EXTLIB_NAME} ${SRCS})

IF(UNIX)
	SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${COMPILE_FLAGS}")
ENDIF()

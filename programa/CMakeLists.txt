CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET(VERSION_LOW 1)
SET(VERSION_UP 0)

PROJECT(FSIA C CXX)

SET(CMAKE_MODULE_PATH "${PROJECT_SOURCE_DIR}/cmk/" ${CMAKE_MODULE_PATH})

#INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/core/fann/include)

ADD_SUBDIRECTORY(core)
ADD_SUBDIRECTORY(demos)
